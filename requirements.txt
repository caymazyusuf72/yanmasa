# Ekran yakalama ve girdi
mss==10.1.0
pillow==12.3.0

# Ajan döngüsü
anthropic==1.0.0
openai>=1.50.0
python-dotenv==1.1.1

# MCP: dış sunucuların araçlarını ajana bağlayan istemci. Sunucular ayrı
# süreçler ve çoğu npx ile geliyor; bu paket yalnızca istemci tarafı.
mcp==2.1.1

# UI Automation yan kanalı
uiautomation==2.0.29

# Terminal: ConPTY oturumu ve ekranı metne çeviren emülatör
pywinpty==3.0.5
pyte==0.8.2

# Kendi ofisimiz — Microsoft Office kurulu değil, gerekmiyor
openpyxl==3.1.5
python-docx==1.2.0
# Formül motoru. Ajanın formül sonucunu kafadan hesaplayıp yanlış söylemesini
# önleyen tek şey bu; isteğe bağlı değil.
formulas==1.3.4

# Masaüstü arayüzü
PySide6==6.11.2
# Kod görünümünün sözdizimi renklendirmesi. Elle yazılmış düzenli ifadeler
# çok satırlı metinlerde yanılıyordu ve ajan her dilde kod yazabiliyor.
pygments==2.21.0

# Test
pytest==9.0.1

# --- Ses ---------------------------------------------------------------
# Ses motoru henüz bağlı değil ve bu paketler kurulu değil. Zorunlu listeye
# koymak, hiç kullanılmayan yüzlerce megabaytı her kuruluma yüklerdi.
# Ses bağlandığında bunlar yukarı taşınacak:
#
#   faster-whisper==1.2.1
#   sounddevice==0.5.2
#   webrtcvad-wheels==2.0.14
#   httpx>=0.28
